{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <h3>Categories</h3>

    <table class="list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <a class="buttonAdd" href="{{ path('category_new') }}">Create new</a>
                    <a class="btn-view" href="{{ path('category_show', {'id': category.id}) }}">show</a>
                    <a class="btn-update" href="{{ path('category_edit', {'id': category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_new') }}">Create new</a>
{% endblock %}
